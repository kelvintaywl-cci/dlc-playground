version: 2.1

jobs:
  build:
    docker:
      - image: naoyer/sample-git:latest
    resource_class: medium
    steps:
      - run: git --version
      - run: ssh -V
      - checkout

workflows:
  main:
    jobs:
      - build
